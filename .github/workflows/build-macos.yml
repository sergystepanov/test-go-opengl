name: Build (macOS)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:
    name: Build
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: 'stable'

      - run: |
          brew --cask xquartz sdl2 freeglut

      - name: Test
        run: |
          #go test -run TestSDL2 ./app
          # go test -run TestGLFW ./app
          go test -run TestGlut ./app
